services:
  - type: web
    name: stock-news-monitor
    env: python
    buildCommand: ""
    startCommand: python main.py
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: stock-news-db
          property: connectionString

databases:
  - name: stock-news-db
    plan: free
